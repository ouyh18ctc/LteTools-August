<UserControl x:Class="Lte.WinApp.Controls.MenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Grid>
        <Button Name="Me" Width="273" Height="120" Margin="5">
            <Grid Width="273" Height="120">
                <Image Source="../Resources/Images/h3-bg.gif" Grid.ColumnSpan="2" Grid.RowSpan="2"/>
                <Image Source="../Resources/Images/arrow.gif"/>
                <Image Source="../Resources/Images/bulls-eye.gif" Grid.Column="1" Grid.Row="1"/>
                <Label Name="MyTitle" Grid.Column="1" FontWeight="Bold" FontSize="14"/>
                <TextBlock Name="MyContent" Grid.Column="1" Grid.Row="1" Margin="10" 
                               TextWrapping="WrapWithOverflow" LineHeight="18"/>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="30" />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition />
                </Grid.RowDefinitions>
            </Grid>
        </Button>
    </Grid>
</UserControl>
